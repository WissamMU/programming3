
# تلخيص شامل للفصل الخامس: الاستثناءات في بايثون

## 📋 **المفاهيم الأساسية**
- **الاستثناءات**: أخطاء تحدث أثناء تنفيذ البرنامج في حالات استثنائية
- **معالجة الاستثناءات**: آلية للتعامل مع الأخطاء دون إيقاف البرنامج
- **الكلمات المفتاحية**: `try`, `except`, `finally`

## 🎯 **أهداف الفصل**
- فهم كيفية معالجة الاستثناءات في بايثون
- استخدام `try`, `except`, `finally` للتحكم بمسار التنفيذ
- كتابة برامج مرنة تستمر في العمل بعد حدوث الأخطاء

## 🔧 **آلية عمل الاستثناءات**

### البنية الأساسية:
```python
try:
    # الكود الذي قد يطلق استثناء
except:
    # معالجة الاستثناء
finally:
    # كود ينفذ في جميع الحالات
```

### البنية المتقدمة:
```python
try:
    # ...
except ValueError as e:
    # معالجة استثناء القيمة
except ZeroDivisionError as e:
    # معالجة استثناء القسمة على صفر
except:
    # معالجة جميع الاستثناءات الأخرى
finally:
    # كود نهائي
```


# 📊 **تفصيل أنواع الاستثناءات في بايثون**

## **الاستثناءات الشائعة وتفسيرها**

| نوع الاستثناء | الوصف التفصيلي | أمثلة واقعية |
|---------------|----------------|--------------|
| **ZeroDivisionError** | يُطلق عند محاولة قسمة عدد على الصفر | `10 / 0`<br>`x = 0; result = 100 / x` |
| **ValueError** | يُطلق عند تمرير قيمة غير مناسبة لدالة ما | `int('hello')`<br>`float('1O')` (حرف O بدل صفر)<br>`int('12.5')` |
| **IndexError** | يُطلق عند الوصول لمؤشر خارج نطاق list/tuple | `lst = [1,2,3]; print(lst[5])`<br>`text = "hello"; print(text[10])` |
| **TypeError** | يُطلق عند تطبيق عملية على نوع بيانات غير مناسب | `'2' + 2` (دمج نص مع رقم)<br>`len(42)` (طول عدد)<br>`"hello" - "world"` |
| **NameError** | يُطلق عند استخدام متغير غير معرّف | `print(undefined_var)`<br>`x = y + 5` (مع أن y غير معرّف) |
| **FileNotFoundError** | يُطلق عند محاولة فتح ملف غير موجود | `open('missing_file.txt', 'r')`<br>`os.remove('nonexistent.file')` |
| **Exception** | **الأب لجميع الاستثناءات** - يلتقط أي استثناء | `try: ... except Exception: ...` |


## 💡 **أمثلة عملية مفصلة**

### **ValueError بالتفصيل:**
```python
# أمثلة تسبب ValueError:
int('abc')           # نص لا يمكن تحويله لعدد
float('12.34.56')    # تنسيق عشري غير صحيح
int('')              # نص فارغ
list.index(99)       # بحث عن عنصر غير موجود
```

### **TypeError بالتفصيل:**
```python
# أمثلة تسبب TypeError:
'Hello' + 42         # دمج نص مع عدد
[1,2,3]['key']       # استخدام مفتاح نصي مع list
len(3.14)            # طول عدد عشري
"text" * "text"      # ضرب نصين
```

### **IndexError بالتفصيل:**
```python
# أمثلة تسبب IndexError:
lst = [1,2,3]
print(lst[3])        # المؤشر يبدأ من 0، العناصر: 0,1,2 فقط
print(lst[-4])       # مؤشر سالب خارج النطاق
lst.pop(5)           # حذف عنصر غير موجود
```

## 🛡️ **نماذج معالجة متقدمة**

### معالجة متعددة الاستثناءات:
```python
try:
    # كود قد يسبب أخطاء متعددة
    x = int(input("أدخل عدداً: "))
    result = 100 / x
    print(f"الناتج: {result}")
    
except ValueError as ve:
    print(f"خطأ في القيمة: {ve}")
except ZeroDivisionError as zde:
    print(f"خطأ في القسمة: {zde}")
except Exception as e:
    print(f"خطأ عام: {e}")
```

### استخدام Exception كشبكة أمان:
```python
try:
    # كود معقد
    process_data()
    
except FileNotFoundError:
    print("الملف غير موجود")
except (ValueError, TypeError):
    print("خطأ في نوع البيانات")
except Exception as e:
    print(f"خطأ غير متوقع: {e}")  # يلتقط أي استثناء آخر
```

## 🏆 **النقاط الهامة**

- ✅ **الاستثناءات** - أخطاء تحدث أثناء التنفيذ في حالات استثنائية
- ✅ **try-except** - التقاط ومعالجة الاستثناءات
- ✅ **finally** - كود ينفذ بغض النظر عن حدوث الاستثناء
- ✅ **تعدد except** - معالجة أنواع مختلفة من الاستثناءات
- ✅ **المرونة** - استمرار عمل البرنامج بعد الأخطاء
- ✅ **التنظيم** - فصل الكود العادي عن كود معالجة الأخطاء

## 🛠 **الكلمات المحجوزة الجديدة**
- `try` - بداية كتلة محاولة التنفيذ
- `except` - معالجة الاستثناءات
- `finally` - كود تنفيذ نهائي

## 📝 **التوابع المستخدمة**
- `int()`, `float()`, `str()` - تحويل الأنواع
- `input()` - إدخال البيانات
- `open()` - فتح الملفات
