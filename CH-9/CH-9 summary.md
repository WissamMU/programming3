# ملخص شامل للفصل التاسع: المكتبات math و matplotlib

## 📋 **المفاهيم الأساسية**
- **مكتبة math**: مكتبة بايثون القياسية للعمليات الرياضية
- **حزمة matplotlib**: حزمة برمجية للرسم البياني والتخطيط
- **مكتبة pyplot**: جزء من matplotlib مخصص للرسم البسيط والمباشر

## 🎯 **أهداف تعليمية**
### الطالب يتعرف على:
- مكتبة math والعمليات الرياضية الجاهزة
- حزمة matplotlib ومكتبة pyplot للرسم البياني
- دوال plot و scatter للرسم
- خصائص الرسم (لون، ثخانة، نمط)

### الطالب يتمكن من:
- استخدام التوابع والثوابت الرياضية الجاهزة
- رسم المنحنيات والنقاط المتناثرة
- تغيير خصائص الرسومات (ألوان، أنماط، عناوين)
- إنشاء رسومات متعددة وأشكال معقدة

## 📊 **مكتبة math**

### **الثوابت الأساسية:**
```python
import math
print(math.e)     # 2.71828... (عدد أويلر)
print(math.pi)    # 3.14159... (باي)
print(math.inf)   # اللانهاية الموجبة
print(math.nan)   # ليس رقماً (Not a Number)
```

### **الدوال الرياضية الأساسية:**

| الدالة | الوصف | مثال |
|--------|--------|-------|
| `sqrt(x)` | الجذر التربيعي | `math.sqrt(9) = 3.0` |
| `pow(x, y)` | الأسس | `math.pow(2, 3) = 8.0` |
| `ceil(x)` | التقريب للأعلى | `math.ceil(9.2) = 10.0` |
| `floor(x)` | التقريب للأسفل | `math.floor(9.8) = 9.0` |
| `sin(x)` | الجيب (بالراديان) | `math.sin(0) = 0.0` |
| `cos(x)` | جيب التمام | `math.cos(0) = 1.0` |
| `exp(x)` | الأس الطبيعي (e^x) | `math.exp(1) ≈ 2.718` |
| `log(x)` | اللوغاريتم الطبيعي | `math.log(2.718) ≈ 1.0` |
| `radians(x)` | تحويل من درجة إلى راديان | `math.radians(180) = π` |

### **ملاحظات مهمة:**
- دالة `abs()` ليست في مكتبة math بل هي دالة مسبقة التعريف في بايثون
- دالة `max()` و `min()` دوال مسبقة التعريف
- `math.pow(x, y)` تؤدي نفس عمل `x ** y` لكنها أكثر دقة

## 📈 **حزمة matplotlib ومكتبة pyplot**

### **الاستيراد الأساسي:**
```python
import matplotlib.pyplot as plt
```

### **1. الرسم الأساسي:**
```python
# رسم منحني باستخدام قائمتين
xpoints = [-8, -7, -5, -4, -2, 0, 2, 4, 5, 7, 8]
ypoints = [64, 49, 25, 16, 4, 0, 4, 16, 25, 49, 64]
plt.plot(xpoints, ypoints)
plt.show()
```

### **2. خصائص الخطوط:**
```python
# تغيير لون ونمط الخط
plt.plot(ypoints, linestyle=':', color='g')  # خط منقط أخضر
plt.plot(ypoints, linestyle='--', color='r') # خط متقطع أحمر

# تغيير ثخانة الخط
plt.plot(xpoints, ypoints, linewidth=9.5)
```

### **3. إضافة العناوين:**
```python
plt.title("عنوان الرسم البياني")
plt.xlabel("المحور X")
plt.ylabel("المحور Y")
plt.show()
```

### **4. تخصيص مجال الرسم:**
```python
plt.xlim(50, 150)  # تحديد نطاق المحور X
plt.ylim(-0.75, 1) # تحديد نطاق المحور Y
```

### **5. رسومات متعددة:**
```python
# رسم أكثر من منحني في نفس الشكل
plt.plot(xpoints, '--', color="r")
plt.plot(ypoints, linewidth='5')
plt.show()
```

### **6. أشكال متعددة (subplot):**
```python
plt.subplot(1, 3, 1)  # صف واحد، 3 أعمدة، الشكل الأول
plt.plot(xpoints)
plt.title("الرسم 1")

plt.subplot(1, 3, 2)  # الشكل الثاني
plt.plot(ypoints)
plt.title("الرسم 2")

plt.subplot(1, 3, 3)  # الشكل الثالث
plt.plot(zpoints)
plt.title("الرسم 3")
```

### **7. رسم النقاط المتناثرة (scatter):**
```python
x = [6, 8, 3, 5, 7, 12, 1, 19, 14, 2, 1, 19, 16]
y = [45, 67, 23, 67, 12, 89, 100, 34, 56, 67, 32, 43, 21]
plt.scatter(x, y)
plt.show()
```

## 💻 **تمارين عملية من الفصل**

### **التمرين 1: تحويل درجات الحرارة**
```python
def CelsiusToFahrenheit(Tc):
    Tf = (9/5) * Tc + 32
    return Tf

listofTc = [Tc for Tc in range(-50, 100)]
listofTf = [CelsiusToFahrenheit(Tc) for Tc in listofTc]

plt.plot(listofTc, listofTf)
plt.title("Fahrenheit VS Celsius")
plt.xlabel("Temperature(celsius)")
plt.ylabel("Temperature(Fahrenheit)")
plt.grid()
plt.show()
```

### **التمرين 2: رسم تابع الفراشة**
```python
def Calc_r(teta):
    r = math.exp(math.sin(teta)) - 2*math.cos(4*teta) + (math.sin((2*teta-math.pi)/24))**5
    return r

def Calc_X(teta):
    r = Calc_r(teta)
    X = r*math.cos(teta)
    return X

def Calc_Y(teta):
    r = Calc_r(teta)
    Y = r*math.sin(teta)
    return Y

xList = [Calc_X(math.radians(z)) for z in range(0,40000)]
yList = [Calc_Y(math.radians(z)) for z in range(0,40000)]

plt.plot(xList, yList)
plt.title("Butterfly")
plt.show()
```

### **التمرين 3: رسم توابع يحددها المستخدم**
```python
while True:
    strF = input("Enter Function of x: ")
    if strF == "end":
        break
    
    minx = int(input("Enter minx: "))
    maxx = int(input("Enter maxx: "))
    
    xpoints = [eval(strF) for x in range(minx, maxx)]
    
    plt.title("Your function: " + strF)
    plt.plot(xpoints)
    plt.show()
```

## 📝 **كلمات مفتاحية ودوال مهمة**

### **الكلمات المحجوزة في بايثون:**
- `import`, `from`, `as` - لاستيراد المكتبات
- `and`, `or`, `not` - عوامل منطقية
- `if`, `elif`, `else` - شروط
- `for`, `while` - حلقات
- `def`, `return` - تعريف الدوال

### **الدوال المسبقة التعريف المستخدمة:**
- `abs()`, `pow()`, `round()` - دوال رياضية
- `len()`, `max()`, `min()`, `sum()` - دوال على التجميعات
- `input()`, `print()`, `eval()` - إدخال وإخراج
- `dir()` - عرض محتويات المكتبات

## 🎓 **نقاط مهمة يجب تذكرها**

1. **استيراد المكتبات:**
   - `import math` للعمليات الرياضية
   - `import matplotlib.pyplot as plt` للرسم البياني

2. **العمل مع math:**
   - جميع الدوال تعيد قيم float
   - الزوايا تكون بالراديان (استخدم radians() للتحويل)

3. **العمل مع matplotlib:**
   - `plot()` للخطوط، `scatter()` للنقاط المتناثرة
   - `show()` لإظهار الرسم
   - يمكن تخصيص الألوان والأنماط والثخانات

4. **أفضل الممارسات:**
   - أضف عناوين واضحة للرسومات
   - حدد نطاقات المحاور إذا لزم الأمر
   - استخدم subplot لتنظيم الرسومات المتعددة

## ✅ **الفوائد والتطبيقات**
1. **math**: حل المشكلات الرياضية المعقدة بسهولة
2. **matplotlib**: إنشاء رسومات بيانية احترافية
3. **التكامل**: استخدام المكتبتين معاً لتمثيل البيانات رياضياً وبصرياً
4. **المرونة**: رسم أي دالة رياضية يريدها المستخدم

هذا الفصل يمنح الطالب الأدوات الأساسية لمعالجة البيانات الرياضية وتمثيلها بيانياً، وهي مهارات أساسية في مجال البرمجة والهندسة.