
# ملخص مفصل وموسع للفصل السابع: البرمجة غرضية التوجه (OOP) في بايثون

## 📚 **التمهيد والنظرة العامة**

### **ماهية البرمجة غرضية التوجه (OOP)**
- **النموذج البرمجي**: يعتمد على مفهوم "الكائنات" التي تجمع بين البيانات والسلوكيات في كيان واحد
- **التناقض مع البرمجة الإجرائية**: حيث تكون البيانات والوظائف منفصلة
- **المبدأ الأساسي**: كل كائن له هوية وحالة وسلوك خاص به

### **مقارنة بين البرمجة الإجرائية والبرمجة غرضية التوجه**
| الجانب | البرمجة الإجرائية | البرمجة غرضية التوجه |
|--------|-------------------|---------------------|
| التركيز | الوظائف والإجراءات | الكائنات والتفاعلات |
| التنظيم | حول الوظائف | حول الكائنات والبيانات |
| البيانات | منفصلة عن الوظائف | مجمعة مع الوظائف في كائنات |
| إعادة الاستخدام | محدودة | عالية (الوراثة، التغليف) |
| الصيانة | أصعب مع نمو البرنامج | أسهل (التعديل في مكان واحد) |

### **لماذا OOP مهمة؟**
- ✅ **إعادة الاستخدام (Reusability)**: يمكن استخدام الكود في مشاريع متعددة
- ✅ **التنظيم (Organization)**: تنظيم الكود في وحدات منطقية
- ✅ **الصيانة (Maintenance)**: سهولة إيجاد وتصحيح الأخطاء
- ✅ **التجريد (Abstraction)**: إخفاء التفاصيل المعقدة عن المستخدم
- ✅ **المرونة (Flexibility)**: سهولة التعديل والتوسيع
- ✅ **الأمان (Security)**: التغليف يحمي البيانات من التعديل المباشر

## 🏗️ **الهيكل الأساسي للصفوف في بايثون**

### **1. تعريف الصف (Class Definition)**
```python
class ClassName:
    """توثيق الصف (اختياري)"""
    
    # متغيرات الصف (Class Variables)
    class_variable = "قيمة مشتركة"
    
    # المنشئ (Constructor)
    def __init__(self, parameter1, parameter2):
        """تهيئة الكائن"""
        self.instance_variable1 = parameter1  # متغير مثيل
        self.instance_variable2 = parameter2
    
    # الطرق (Methods)
    def instance_method(self):
        """طريقة المثيل"""
        return f"القيمة: {self.instance_variable1}"
    
    @staticmethod
    def static_method():
        """طريقة ثابتة"""
        return "أنا طريقة ثابتة"
    
    @classmethod
    def class_method(cls):
        """طريقة الصف"""
        return f"متغير الصف: {cls.class_variable}"
```

### **2. مقارنة مكونات الصف**
| المكون | الوصف | أمثلة | متى يستخدم |
|--------|-------|-------|------------|
| **متحولات الصف** | قيم مشتركة بين جميع الكائنات | `counter = 0` | عندما تحتاج قيمة مشتركة |
| **متحولات المثيل** | قيم خاصة بكل كائن | `self.name`, `self.age` | لتمثيل حالة الكائن |
| **المنشئ** | يُستدعى عند إنشاء الكائن | `def __init__(self, ...)` | لتهيئة الكائن |
| **طرائق المثيل** | تعمل على كائن محدد | `def method(self, ...)` | للعمليات على الكائن |
| **طرائق الصف** | تعمل على الصف نفسه | `@classmethod def method(cls, ...)` | للعمليات على الصف |
| **الطرائق الثابتة** | لا تحتاج لكائن أو صف | `@staticmethod def method(...)` | لوظائف مساعدة |

### **3. شرح المكونات الأساسية**

#### **أ. المنشئ (Constructor) - `__init__`**
- **الوظيفة**: تهيئة الكائن عند إنشائه
- **التنفيذ**: يُستدعى تلقائياً عند إنشاء كائن جديد
- **المعاملات**: أول معامل دائمًا هو `self`
- **الاستخدام**: لتعيين القيم الأولية لمتغيرات المثيل
- **ملاحظة هامة**: في بايثون، لا يوجد **overloading** للمنشئ

```python
class Student:
    def __init__(self, name, student_id, gpa=0.0):
        """
        تهيئة كائن الطالب
        
        Args:
            name (str): اسم الطالب
            student_id (int): رقم الطالب الجامعي
            gpa (float): المعدل التراكمي (اختياري، افتراضي 0.0)
        """
        self.name = name
        self.id = student_id
        self.gpa = gpa
        self.courses = []  # قائمة فارغة للمقررات
```

#### **ب. الكلمة المفتاحية `self`**
- **المعنى**: تشير إلى **الكائن الحالي**
- **الضرورة**: مطلوبة في جميع طرق المثيل
- **الاستخدام**: للوصول إلى متغيرات وطرق الكائن
- **في المنشئ**: تستخدم لإنشاء متغيرات المثيل
- **في الطرق**: تستخدم للوصول إلى متغيرات وطرق الكائن

```python
class Account:
    def __init__(self, account_number, balance=0):
        self.account_number = account_number
        self.balance = balance
    
    def deposit(self, amount):
        """إيداع مبلغ في الحساب"""
        if amount > 0:
            self.balance += amount
            print(f"تم إيداع {amount}. الرصيد الحالي: {self.balance}")
        return self.balance  # إرجاع self.balance للكائن الحالي
```

## 🔄 **أنواع الطرق (Methods) في الصفوف**

### **1. طرق المثيل (Instance Methods)**
- **الوصف**: طرق تعمل على كائنات محددة من الصف
- **المعامل الأول**: دائمًا `self` (يشير إلى الكائن الحالي)
- **الوصول**: تحتاج إلى إنشاء كائن قبل استدعائها
- **الاستخدام**: للعمليات التي تتعلق بكائن معين
- **مثال**: `object.method()`

```python
class Rectangle:
    def __init__(self, length, width):
        self.length = length
        self.width = width
    
    def area(self):
        """حساب مساحة المستطيل"""
        return self.length * self.width
    
    def perimeter(self):
        """حساب محيط المستطيل"""
        return 2 * (self.length + self.width)
    
    def is_square(self):
        """التحقق إذا كان المستطيل مربعًا"""
        return self.length == self.width
```

### **2. الطرق الثابتة (Static Methods)**
- **الوصف**: طرق لا تحتاج إلى كائن ولا إلى صف لتعمل
- **المعامل الأول**: لا يوجد `self` أو `cls`
- **الوصول**: يمكن استدعاؤها مباشرة من الصف بدون إنشاء كائن
- **الاستخدام**: للوظائف المنطقية المنفصلة عن حالة الكائن
- **الفوائد**: تنظيم الوظائف ذات الصلة، تجنب التلوث العالمي
- **التركيب**: تُزين بـ `@staticmethod`

```python
class MathOperations:
    @staticmethod
    def add(a, b):
        """جمع رقمين"""
        return a + b
    
    @staticmethod
    def factorial(n):
        """حساب مضروب عدد"""
        if n < 0:
            return None
        result = 1
        for i in range(1, n + 1):
            result *= i
        return result
    
    @staticmethod
    def is_prime(n):
        """التحقق إذا كان عدد أولي"""
        if n < 2:
            return False
        for i in range(2, int(n ** 0.5) + 1):
            if n % i == 0:
                return False
        return True

# الاستخدام بدون إنشاء كائن
result = MathOperations.add(5, 3)  # 8
fact = MathOperations.factorial(5)  # 120
```

### **3. طرق الصف (Class Methods)**
- **الوصف**: طرق تعمل على الصف نفسه وليس على كائناته
- **المعامل الأول**: `cls` (يشير إلى الصف نفسه)
- **الوصول**: يمكن استدعاؤها مباشرة من الصف
- **الاستخدام**: لإنشاء كائنات بطرق بديلة، التعامل مع متغيرات الصف
- **التركيب**: تُزين بـ `@classmethod`

```python
class Car:
    total_cars = 0  # متغير صف
    
    def __init__(self, brand, model):
        self.brand = brand
        self.model = model
        Car.total_cars += 1
    
    @classmethod
    def get_total_cars(cls):
        """الحصول على عدد السيارات المصنوعة"""
        return cls.total_cars
    
    @classmethod
    def from_string(cls, car_string):
        """إنشاء كائن سيارة من سلسلة نصية"""
        brand, model = car_string.split('-')
        return cls(brand, model)
    
    @classmethod
    def reset_counter(cls):
        """إعادة عداد السيارات إلى الصفر"""
        cls.total_cars = 0

# استخدام طرق الصف
car1 = Car("Toyota", "Camry")
car2 = Car.from_string("Honda-Civic")

print(Car.get_total_cars())  # 2
Car.reset_counter()
print(Car.get_total_cars())  # 0
```

### **4. مقارنة شاملة بين أنواع الطرق**
| الخاصية | طريقة المثيل | طريقة الصف | الطريقة الثابتة |
|---------|---------------|-------------|-----------------|
| **المعامل الأول** | `self` (الكائن) | `cls` (الصف) | لا يوجد |
| **الوصول للمتغيرات** | ✅ `self.variable` | ✅ `cls.variable` | ❌ لا يمكن |
| **الوصول للطرق** | ✅ `self.method()` | ✅ `cls.method()` | ❌ لا يمكن |
| **إنشاء كائنات** | ❌ لا يمكن | ✅ `cls()` | ❌ لا يمكن |
| **التزيين** | غير مطلوب | `@classmethod` | `@staticmethod` |
| **مثال الاستدعاء** | `obj.method()` | `ClassName.method()` | `ClassName.method()` |
| **الاستخدام الشائع** | عمليات على الكائن | مصنع كائنات، إحصائيات | وظائف مساعدة |

## 🎭 **محددات الوصول (Access Modifiers)**

### **1. المستويات الثلاثة للوصول في البرمجة غرضية التوجه**
| المستوى | الوصف | في بايثون | مثال |
|---------|-------|-----------|------|
| **Public (عام)** | متاح من أي مكان | بدون بادئة | `self.name` |
| **Protected (محمي)** | متاح للصف وأبنائه | `_` (شرطة سفلية واحدة) | `self._id` |
| **Private (خاص)** | متاح داخل الصف فقط | `__` (شرطتين سفليتين) | `self.__salary` |

### **2. التنفيذ في بايثون**
```python
class Employee:
    def __init__(self, name, salary, department):
        self.name = name            # public - عام
        self._department = department  # protected - محمي
        self.__salary = salary      # private - خاص
    
    def get_salary(self):
        """getter للراتب الخاص"""
        return self.__salary
    
    def set_salary(self, new_salary):
        """setter للراتب الخاص"""
        if new_salary >= 0:
            self.__salary = new_salary
        else:
            print("الراتب يجب أن يكون موجبًا")
    
    def _internal_method(self):
        """طريقة محمية للاستخدام الداخلي"""
        return f"القسم: {self._department}"
    
    def __private_calculation(self):
        """طريقة خاصة للحسابات الداخلية"""
        return self.__salary * 1.1  # زيادة 10%

# الاستخدام
emp = Employee("أحمد", 5000, "التطوير")
print(emp.name)          # ✅ متاح: أحمد
print(emp._department)   # ⚠️ متاح لكنه غير مستحسن
# print(emp.__salary)    # ❌ خطأ: غير متاح مباشرة
print(emp.get_salary())  # ✅ الطريقة الصحيحة: 5000
```

### **3. حقيقة الخصوصية في بايثون**
```python
class Secret:
    def __init__(self):
        self.public = "أنا عام"
        self._protected = "أنا محمي"
        self.__private = "أنا خاص"
        self.__very_private__ = "خاص جداً"
    
    def reveal_secrets(self):
        """كشف الأسرار من داخل الصف"""
        print(f"العام: {self.public}")
        print(f"المحمي: {self._protected}")
        print(f"الخاص: {self.__private}")

# حقيقة الخصوصية في بايثون
secret = Secret()
print(secret._Secret__private)  # ✅ الوصول بطريقة غير مباشرة
# بايثون تقوم بتسمية خاصة: _ClassName__variable
```

### **4. جدول مقارنة محددات الوصول**
| الجانب | Public | Protected | Private |
|--------|--------|-----------|---------|
| **البادئة** | لا توجد | `_` | `__` |
| **الوصول من داخل الصف** | ✅ نعم | ✅ نعم | ✅ نعم |
| **الوصول من خارج الصف** | ✅ نعم | ⚠️ ممكن (غير مستحسن) | ❌ غير ممكن |
| **الوصول من الصفوف الوريثة** | ✅ نعم | ✅ نعم | ❌ غير ممكن |
| **تسمية بايثون الداخلية** | كما هي | كما هي | `_ClassName__variable` |
| **الاستخدام الموصى به** | واجهة برمجة التطبيقات | للاستخدام الداخلي | للبيانات الحساسة |

## 🔗 **الوراثة (Inheritance)**

### **1. أنواع الوراثة في البرمجة غرضية التوجه**
| النوع | الوصف | في بايثون | مثال |
|-------|-------|-----------|------|
| **الوراثة البسيطة** | صف يرث من صف أب واحد | `class Child(Parent):` | `class Dog(Animal):` |
| **الوراثة المتعددة** | صف يرث من أكثر من صف أب | `class Child(Parent1, Parent2):` | `class Duck(Flyable, Swimmable):` |
| **الوراثة متعددة المستويات** | تسلسل هرمي للوراثة | `class A:`, `class B(A):`, `class C(B):` | `Point → Circle → Cylinder` |
| **الوراثة الهجينة** | مزيج من أنواع الوراثة | مزيج من الأنواع أعلاه | `class E(A, B, C):` |

### **2. الوراثة البسيطة**
```python
class Animal:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def eat(self):
        return f"{self.name} يأكل"
    
    def sleep(self):
        return f"{self.name} ينام"
    
    def make_sound(self):
        return "صوت الحيوان"

class Dog(Animal):  # Dog يرث من Animal
    def __init__(self, name, age, breed):
        super().__init__(name, age)  # استدعاء منشئ الأب
        self.breed = breed
    
    def make_sound(self):  # إعادة تعريف (override)
        return "هاو هاو!"
    
    def fetch(self):  # طريقة جديدة خاصة بالكلب
        return f"{self.name} يجلب العصا"

class Cat(Animal):  # Cat يرث من Animal
    def __init__(self, name, age, color):
        super().__init__(name, age)
        self.color = color
    
    def make_sound(self):  # إعادة تعريف (override)
        return "مواء!"
    
    def climb(self):  # طريقة جديدة خاصة بالقط
        return f"{self.name} يتسلق الشجرة"
```

### **3. الوراثة المتعددة (Multiple Inheritance)**
```python
class Flyable:
    def __init__(self, max_altitude):
        self.max_altitude = max_altitude
    
    def fly(self):
        return "يطير في السماء"
    
    def land(self):
        return "يهبط على الأرض"

class Swimmable:
    def __init__(self, max_depth):
        self.max_depth = max_depth
    
    def swim(self):
        return "يسبح في الماء"
    
    def dive(self):
        return "يغوص تحت الماء"

class Duck(Flyable, Swimmable):  # يرث من صفين
    def __init__(self, name, max_altitude, max_depth):
        Flyable.__init__(self, max_altitude)
        Swimmable.__init__(self, max_depth)
        self.name = name
    
    def quack(self):
        return "كواك كواك!"

# إنشاء وإستخدام
donald = Duck("دونالد", 100, 10)
print(donald.fly())    # من Flyable
print(donald.swim())   # من Swimmable
print(donald.quack())  # من Duck نفسها
```

### **4. دوال فحص الوراثة في بايثون**
| الدالة | الوصف | الصيغة | مثال |
|--------|-------|--------|------|
| **isinstance()** | التحقق إذا كان الكائن من نوع معين | `isinstance(obj, Class)` | `isinstance(dog, Animal)` |
| **issubclass()** | التحقق من علاقة الوراثة بين صفين | `issubclass(Child, Parent)` | `issubclass(Dog, Animal)` |
| **type()** | معرفة نوع الكائن | `type(obj)` | `type(dog)` |
| **.__class__** | الحصول على صف الكائن | `obj.__class__` | `dog.__class__` |
| **.__bases__** | الحصول على صفوف الأب | `Class.__bases__` | `Dog.__bases__` |
| **.__mro__** | ترتيب دقة الأسلوب (MRO) | `Class.__mro__` | `Duck.__mro__` |

## ✨ **التوابع الجاهزة (Magic/Dunder Methods)**

### **1. التوابع الحسابية والمقارنة**
| العملية | التابع الجاهز | الوصف | مثال الاستخدام |
|---------|---------------|-------|----------------|
| **الجمع** | `__add__(self, other)` | جمع كائنين | `v1 + v2` |
| **الطرح** | `__sub__(self, other)` | طرح كائنين | `v1 - v2` |
| **الضرب** | `__mul__(self, other)` | ضرب كائنين | `v1 * v2` |
| **القسمة** | `__truediv__(self, other)` | قسمة كائنين | `v1 / v2` |
| **القسمة الصحيحة** | `__floordiv__(self, other)` | قسمة صحيحة | `v1 // v2` |
| **الباقي** | `__mod__(self, other)` | باقي القسمة | `v1 % v2` |
| **الأس** | `__pow__(self, other)` | رفع لقوة | `v1 ** v2` |
| **الإزاحة اليمنى** | `__rshift__(self, other)` | إزاحة لليمين | `v1 >> v2` |
| **الإزاحة اليسرى** | `__lshift__(self, other)` | إزاحة لليسار | `v1 << v2` |
| **AND المنطقي** | `__and__(self, other)` | AND منطقي | `v1 & v2` |
| **OR المنطقي** | `__or__(self, other)` | OR منطقي | `v1 \| v2` |
| **XOR المنطقي** | `__xor__(self, other)` | XOR منطقي | `v1 ^ v2` |
| **أقل من** | `__lt__(self, other)` | أقل من | `v1 < v2` |
| **أكبر من** | `__gt__(self, other)` | أكبر من | `v1 > v2` |
| **أقل من أو يساوي** | `__le__(self, other)` | أقل من أو يساوي | `v1 <= v2` |
| **أكبر من أو يساوي** | `__ge__(self, other)` | أكبر من أو يساوي | `v1 >= v2` |
| **يساوي** | `__eq__(self, other)` | يساوي | `v1 == v2` |
| **لا يساوي** | `__ne__(self, other)` | لا يساوي | `v1 != v2` |

### **2. التوابع النصية والتمثيلية**
| التابع | الوصف | متى يُستدعى | مثال |
|--------|-------|-------------|------|
| **`__str__(self)`** | تمثيل نصي للمستخدم | `str(obj)`, `print(obj)` | `print(book)` |
| **`__repr__(self)`** | تمثيل رسمي للمبرمجين | `repr(obj)` | `repr(book)` |
| **`__len__(self)`** | طول الكائن | `len(obj)` | `len(list_obj)` |
| **`__getitem__(self, key)`** | الوصول بالفهرس | `obj[key]` | `list_obj[0]` |
| **`__setitem__(self, key, value)`** | تعيين بالقيمة | `obj[key] = value` | `dict_obj['key'] = value` |
| **`__delitem__(self, key)`** | حذف بالمفتاح | `del obj[key]` | `del dict_obj['key']` |
| **`__iter__(self)`** | الحصول على مكرر | `for item in obj:` | `for x in container:` |
| **`__next__(self)`** | العنصر التالي في التكرار | `next(iterator)` | `next(iter_obj)` |
| **`__contains__(self, item)`** | التحقق من الوجود | `item in obj` | `5 in container` |

### **3. مثال تطبيقي شامل**
```python
class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    
    # العمليات الأساسية
    def __add__(self, other):
        """الجمع: v1 + v2"""
        return Vector(self.x + other.x, self.y + other.y)
    
    def __sub__(self, other):
        """الطرح: v1 - v2"""
        return Vector(self.x - other.x, self.y - other.y)
    
    def __mul__(self, scalar):
        """الضرب: v * scalar"""
        if isinstance(scalar, (int, float)):
            return Vector(self.x * scalar, self.y * scalar)
        raise TypeError("يجب أن يكون المضروب رقمًا")
    
    def __truediv__(self, scalar):
        """القسمة: v / scalar"""
        if scalar != 0:
            return Vector(self.x / scalar, self.y / scalar)
        raise ZeroDivisionError("القسمة على صفر غير مسموحة")
    
    # المقارنات
    def __eq__(self, other):
        """المساواة: v1 == v2"""
        return self.x == other.x and self.y == other.y
    
    def __lt__(self, other):
        """أقل من: v1 < v2 (بالمقدار)"""
        return (self.x**2 + self.y**2) < (other.x**2 + other.y**2)
    
    # التمثيل النصي
    def __str__(self):
        """للطباعة والإخراج"""
        return f"Vector({self.x}, {self.y})"
    
    def __repr__(self):
        """للتصحيح والتطوير"""
        return f"Vector({self.x}, {self.y})"
    
    # العمليات العكسية
    def __radd__(self, other):
        """الجمع العكسي: scalar + v"""
        return self.__add__(other)
    
    def __rmul__(self, scalar):
        """الضرب العكسي: scalar * v"""
        return self.__mul__(scalar)
```

### **4. جدول التوابع الجاهزة الخاصة بدورة الحياة**
| التابع | الوصف | متى يُستدعى | استخدام شائع |
|--------|-------|-------------|--------------|
| **`__new__(cls, ...)`** | إنشاء الكائن | قبل `__init__` | أنماط التصميم |
| **`__init__(self, ...)`** | تهيئة الكائن | عند الإنشاء | تعيين القيم الأولية |
| **`__del__(self)`** | تدمير الكائن | قبل الحذف | تنظيف الموارد |
| **`__call__(self, ...)`** | استدعاء الكائن | `obj()` | الكائنات القابلة للاستدعاء |
| **`__enter__(self)`** | دخول سياق | مع `with` | إدارة الموارد |
| **`__exit__(self, ...)`** | خروج سياق | مع `with` | إدارة الموارد |
| **`__getattr__(self, name)`** | سمة غير موجودة | `obj.missing` | السمات الديناميكية |
| **`__setattr__(self, name, value)`** | تعيين سمة | `obj.attr = value` | التحقق من الصحة |
| **`__delattr__(self, name)`** | حذف سمة | `del obj.attr` | منع حذف سمات |

## 🎯 **المثال الشامل من الكتاب: Time Class**

### **التنفيذ الكامل مع التوثيق**
```python
class Time:
    """
    فئة الوقت لتمثيل وتعامل مع الوقت في صيغ مختلفة
    
    Attributes:
        hour (int): الساعة (0-23)
        minute (int): الدقيقة (0-59)
        second (int): الثانية (0-59)
    """
    
    def __init__(self, hour=0, minute=0, second=0):
        """
        تهيئة كائن الوقت
        
        Args:
            hour (int): الساعة (0-23)
            minute (int): الدقيقة (0-59)
            second (int): الثانية (0-59)
        """
        self.set_time(hour, minute, second)
    
    def set_time(self, hour=0, minute=0, second=0):
        """
        تعيين وقت جديد مع التحقق من الصحة
        
        Args:
            hour (int): الساعة (0-23)
            minute (int): الدقيقة (0-59)
            second (int): الثانية (0-59)
        """
        # التحقق من صحة المدخلات وتعيين القيم الافتراضية
        self.hour = hour if 0 <= hour < 24 else 0
        self.minute = minute if 0 <= minute < 60 else 0
        self.second = second if 0 <= second < 60 else 0
    
    def to_universal_string(self):
        """
        تحويل الوقت إلى صيغة عالمية (24 ساعة)
        
        Returns:
            str: الوقت بصيغة HH:MM:SS
        """
        return f"{self.hour:02d}:{self.minute:02d}:{self.second:02d}"
    
    def to_standard_string(self):
        """
        تحويل الوقت إلى صيغة قياسية (12 ساعة مع AM/PM)
        
        Returns:
            str: الوقت بصيغة HH:MM:SS AM/PM
        """
        # تحديد AM أو PM
        period = "AM" if self.hour < 12 else "PM"
        
        # تحويل الساعة إلى صيغة 12 ساعة
        if self.hour == 0 or self.hour == 12:
            display_hour = 12
        else:
            display_hour = self.hour % 12
        
        return f"{display_hour:02d}:{self.minute:02d}:{self.second:02d} {period}"
    
    def add_seconds(self, seconds):
        """
        إضافة ثواني إلى الوقت
        
        Args:
            seconds (int): عدد الثواني للإضافة
            
        Returns:
            Time: الوقت الجديد بعد الإضافة
        """
        # تحويل الوقت الحالي إلى ثواني
        total_seconds = (self.hour * 3600 + 
                        self.minute * 60 + 
                        self.second + 
                        seconds)
        
        # حساب الوقت الجديد
        new_hour = (total_seconds // 3600) % 24
        new_minute = (total_seconds % 3600) // 60
        new_second = total_seconds % 60
        
        return Time(new_hour, new_minute, new_second)
    
    def __str__(self):
        """تمثيل نصي للوقت"""
        return self.to_standard_string()
    
    def __repr__(self):
        """تمثيل رسمي للوقت"""
        return f"Time({self.hour}, {self.minute}, {self.second})"
    
    def __add__(self, other):
        """جمع وقتين"""
        if isinstance(other, Time):
            total_seconds = (self.hour * 3600 + self.minute * 60 + self.second +
                            other.hour * 3600 + other.minute * 60 + other.second)
            hour = (total_seconds // 3600) % 24
            minute = (total_seconds % 3600) // 60
            second = total_seconds % 60
            return Time(hour, minute, second)
        elif isinstance(other, int):  # إضافة ثواني
            return self.add_seconds(other)
        else:
            raise TypeError("يمكن جمع Time فقط مع Time أو عدد صحيح")
    
    def __eq__(self, other):
        """المساواة بين وقتين"""
        if isinstance(other, Time):
            return (self.hour == other.hour and 
                    self.minute == other.minute and 
                    self.second == other.second)
        return False
    
    def __lt__(self, other):
        """مقارنة وقتين (أقل من)"""
        if isinstance(other, Time):
            self_seconds = self.hour * 3600 + self.minute * 60 + self.second
            other_seconds = other.hour * 3600 + other.minute * 60 + other.second
            return self_seconds < other_seconds
        raise TypeError("يمكن المقارنة فقط مع Time")
```

### **جدول تحويل صيغ الوقت**
| الصيغة | الوصف | مثال | طريقة التحويل |
|--------|-------|------|---------------|
| **24 ساعة** | صيغة عالمية | 14:30:45 | `to_universal_string()` |
| **12 ساعة مع AM/PM** | صيغة قياسية | 02:30:45 PM | `to_standard_string()` |
| **الثواني الإجمالية** | للعمليات الحسابية | 52545 ثانية | تحويل داخلي |
| **كائن Time** | للبرمجة | `Time(14, 30, 45)` | `__repr__()` |

### **3. خصائص البرمجة (Properties)**
```python
class BankAccount:
    def __init__(self, owner, initial_balance=0):
        self._owner = owner
        self._balance = initial_balance
        self._transaction_history = []
    
    @property
    def balance(self):
        """الحصول على الرصيد (getter)"""
        return self._balance
    
    @balance.setter
    def balance(self, amount):
        """تعيين الرصيد (setter)"""
        if amount < 0:
            raise ValueError("الرصيد لا يمكن أن يكون سالبًا")
        old_balance = self._balance
        self._balance = amount
        self._transaction_history.append(f"تعديل رصيد: {old_balance} → {amount}")
    
    @property
    def owner(self):
        """الحصول على اسم المالك (getter فقط)"""
        return self._owner
    
    def deposit(self, amount):
        """إيداع مبلغ"""
        if amount > 0:
            self.balance += amount  # يستخدم الـ setter
            self._transaction_history.append(f"إيداع: +{amount}")
        else:
            raise ValueError("المبلغ يجب أن يكون موجبًا")
    
    def withdraw(self, amount):
        """سحب مبلغ"""
        if 0 < amount <= self.balance:
            self.balance -= amount  # يستخدم الـ setter
            self._transaction_history.append(f"سحب: -{amount}")
        else:
            raise ValueError("المبلغ غير صالح أو غير متوفر")
    
    @property
    def transaction_count(self):
        """عدد المعاملات (خاصية محسوبة)"""
        return len(self._transaction_history)
    
    @property
    def last_transaction(self):
        """آخر معاملة"""
        if self._transaction_history:
            return self._transaction_history[-1]
        return "لا توجد معاملات"
```

### **4. جدول أنواع التوابع في بايثون**
| النوع | التزيين | المعامل الأول | الوصول إلى | مثال الاستدعاء |
|-------|---------|---------------|------------|----------------|
| **Instance Method** | لا شيء | `self` | `self.attr` | `obj.method()` |
| **Class Method** | `@classmethod` | `cls` | `cls.attr` | `Class.method()` |
| **Static Method** | `@staticmethod` | لا يوجد | لا يوجد | `Class.method()` |
| **Property Getter** | `@property` | `self` | `self._attr` | `obj.property` |
| **Property Setter** | `@attr.setter` | `self`, `value` | `self._attr` | `obj.property = value` |
| **Property Deleter** | `@attr.deleter` | `self` | `self._attr` | `del obj.property` |


## 🏆 **الخلاصة النهائية للفصل السابع**

### **1. جدول المفاهيم الأساسية التي تم استيعابها**
| المفهوم | الوصف | أهمية | مثال |
|---------|-------|-------|------|
| **الصفوف (Classes)** | قوالب لإنشاء الكائنات | التنظيم وإعادة الاستخدام | `class Person:` |
| **الكائنات (Objects)** | نسخ من الصفوف تحتوي بيانات | تمثيل الكيانات الحقيقية | `person = Person()` |
| **الوراثة (Inheritance)** | آلية لاستعادة خصائص الصفوف | إعادة استخدام الكود | `class Student(Person):` |
| **التغليف (Encapsulation)** | إخفاء تفاصيل التنفيذ | الأمان والمرونة | `self.__private` |
| **تعدد الأشكال (Polymorphism)** | نفس الواجهة لسلوكيات مختلفة | المرونة والتبسيط | `animal.make_sound()` |
| **المنشئ (Constructor)** | تهيئة الكائنات | تعيين القيم الأولية | `def __init__(self):` |
| **التوابع الجاهزة** | زيادة تحميل العمليات | سلوكيات مخصصة | `__add__`, `__str__` |

### **2. جدول المهارات المكتسبة**
| المهارة | الوصف | كيفية التطبيق | النتيجة |
|---------|-------|---------------|---------|
| **تصميم الصفوف** | إنشاء هياكل منطقية | تحليل المتطلبات | كود منظم |
| **تنفيذ الوراثة** | بناء تسلسلات هرمية | استخدام `super()` | إعادة استخدام |
| **زيادة تحميل المشغلين** | تخصيص سلوك العمليات | التوابع الجاهزة | سلوك مخصص |
| **تطبيق مبادئ OOP** | التجريد والتغليف | تصميم الواجهات | كود نظيف |
| **إدارة حالات الكائنات** | التحكم في البيانات | properties و getters/setters | أمان البيانات |

### **3. جدول الاختلافات المهمة في بايثون**
| الجانب | بايثون | لغات أخرى (Java/C#) | التفسير |
|--------|--------|---------------------|---------|
| **محددات الوصول** | اصطلاحية | إلزامية | مرونة أكثر |
| **الوراثة المتعددة** | ✅ مدعومة | ❌ غير مدعومة | قوة بايثون |
| **التوابع الجاهزة** | نظام كامل | محدود | تحكم أكبر |
| **البرمجة الديناميكية** | ✅ سهل | ❌ صعب | مرونة التنفيذ |
| **كلمة self** | إلزامية | `this` (ضمني) | وضوح أكثر |
| **المنشئ** | `__init__` | نفس اسم الصف | توحيد |

### **4. جدول أفضل الممارسات**
| الممارسة | الوصف | كيفية التطبيق | الفائدة |
|----------|-------|---------------|---------|
| **أسماء واضحة** | وصفية ومفهومة | `CustomerAccount` بدلاً من `CA` | قراءة أفضل |
| **توثيق شامل** | شرح الوظيفة | docstrings و comments | صيانة أسهل |
| **تقسيم المسؤوليات** | كل صف له وظيفة | فصل المهام | تنظيم أفضل |
| **استخدام الوراثة** | عند وجود علاقة "is-a" | `Dog` is an `Animal` | إعادة استخدام |
| **اختبار الصفوف** | اختبار كل صف | `unittest` | جودة أعلى |
| **التجريد** | إخفاء التعقيد | واجهات بسيطة | سهولة الاستخدام |

### **5. جدول الكلمات المفتاحية والمحجوزة المهمة**
| الكلمة | الوصف | الفصل | مثال |
|--------|-------|-------|------|
| **class** | تعريف الصف | 7 | `class Person:` |
| **def** | تعريف دالة/طريقة | 5, 7 | `def method(self):` |
| **self** | الإشارة للكائن الحالي | 7 | `self.name = name` |
| **__init__** | المنشئ | 7 | `def __init__(self):` |
| **super()** | الوصول لصف الأب | 7 | `super().__init__()` |
| **@staticmethod** | طريقة ثابتة | 7 | `@staticmethod def func():` |
| **@classmethod** | طريقة صف | 7 | `@classmethod def func(cls):` |
| **@property** | خاصية | 7 | `@property def attr(self):` |
| **isinstance** | التحقق من النوع | 7 | `isinstance(obj, Class)` |
| **issubclass** | التحقق من الوراثة | 7 | `issubclass(Child, Parent)` |

يُمكنّن هذا الفصل المبرمجين من بناء أنظمة برمجية معقدة وقابلة للصيانة، مع التركيز على المبادئ الأساسية للبرمجة غرضية التوجه وتطبيقها الفعلي في بيئة بايثون.