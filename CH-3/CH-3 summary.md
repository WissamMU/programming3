# مقرر برمجة 3 بايثون
## الفصل الثالث: السلاسل الحرفية String والملفات النصية Text Files
---

### **المفاهيم الأساسية**
- **السلاسل الحرفية**: التعريف، الطرائق والعمليات عليها
- **الملفات النصية**: كتابتها وقراءتها
- **إدارة السياق باستخدام with**: الطريقة الآمنة للتعامل مع الملفات

### **ملخص**
تحتاج العديد من التطبيقات إلى التعامل مع السلاسل الحرفية: صياغة سلسلة، البحث ضمنها، تقطيعها.  
تتضمن بايثون كل ما يلزم من العمليات على السلاسل الحرفية.  
الملفات النصية هي وسيلة التخزين الدائم المعيارية لتبادل المعلومات بين مختلف البرامج.

---

## **1. السلاسل الحرفية Strings**

### **1.1 تعريف السلاسل الحرفية**
```python
newString = "This is a string literal"
literalOne = "\\\\MySystem\\MyDirectory\\ProgrammingC#.cs"
verbatimLiteralOne = r"\\MySystem\MyDirectory\ProgrammingC#.cs"
literalTwo = "Line One\nLine Two"
```

**ملاحظات مهمة:**
- تستخدم `\` للدلالة على أن المحرف التالي يجب أن يُعتبر كما هو وليس محرف خاص
- تستخدم `r` لإلغاء جميع المحارف الخاصة ضمن السلسلة
- `\n` تعني سطر جديد
- **السلاسل في بايثون غير قابلة للتعديل (immutable)** - أي أن `str[i] = x` يولد خطأ
- تستخدم الأدلة في السلاسل الحرفية كما في القوائم `list[i]`

### **1.2 صياغة السلاسل الحرفية**

#### **طريقة الجمع (Concatenation)**
```python
newString = "Jon has " + str(34) + " Books"
```

#### **طريقة format() - الأكثر مرونة**
```python
newString1 = "Jon has {} books".format(34)
newString2 = "Jon has {x} books".format(x=34)
newString3 = "Jon has {0} books and {1} pen".format(34, 45)

# تنسيق الأرقام
x = 123
print("The value is {:6d}".format(x))      # The value is    123
print("The value is {:06d}".format(x))     # The value is 000123

x = 123.4567
print("The value is {:12.6f}".format(x))   # The value is   123.456700
print("The value is {:12,.3f}".format(x))  # The value is     123.457
```

#### **طريقة printf-style (مشابهة للغة C)**
```python
newString1 = "Jon has %d books" % 34
newString3 = "Jon has %d books and %d pen" % (34, 45)

x = 123
print("The value is %6d" % x)              # The value is    123
print("The value is %06d" % x)             # The value is 000123

x = 123.4567
print("The value is %12.6f" % x)           # The value is   123.456700
print("The value is %12.3f" % x)           # The value is     123.457
```

### **1.3 أهم الطرائق والعمليات على السلاسل**

| الطريقة/العملية | الوصف | مثال |
|-----------------|-------|------|
| `==, <, <=, >, >=` | مقارنة بين السلاسل (مراعية حالة الحرف) | `"abcd" > "ABCD"` → `True` |
| `+` | جمع سلسلتين نصيتين | `"ab" + "cd"` → `"abcd"` |
| `str1.endswith(str2)` | اختبار انتهاء السلسلة بسلسلة أخرى | `"hello".endswith("lo")` → `True` |
| `str1.startswith(str2)` | اختبار ابتداء السلسلة بسلسلة أخرى | `"hello".startswith("he")` → `True` |
| `str1.index("sub")` | فهرس أول ظهور لسلسلة ضمن سلسلة | `"hello".index("ll")` → `2` |
| `str1.rindex("sub")` | فهرس آخر ظهور لسلسلة ضمن سلسلة | `"hello".rindex("l")` → `3` |
| `len(str1)` | عدد محارف السلسلة النصية | `len("hello")` → `5` |
| `str1.split(sep=" ")` | تقطيع سلسلة إلى قائمة سلاسل | `"a b c".split()` → `['a','b','c']` |
| `str1.lower()` | التحويل لأحرف صغيرة | `"Hello".lower()` → `"hello"` |
| `str1.upper()` | التحويل لأحرف كبيرة | `"hello".upper()` → `"HELLO"` |
| `str1.strip()` | حذف الفراغات الزائدة من البداية والنهاية | `" hello ".strip()` → `"hello"` |
| `str1.lstrip()` | حذف الفراغات الزائدة من البداية فقط | `" hello ".lstrip()` → `"hello "` |
| `str1.rstrip()` | حذف الفراغات الزائدة من النهاية فقط | `" hello ".rstrip()` → `" hello"` |
| `",".join(list)` | دمج عناصر قائمة بسلسلة فصل | `",".join(['a','b','c'])` → `"a,b,c"` |
| `str1[3:]` | سلسلة جزئية من الفهرس 3 للنهاية | `"hello"[3:]` → `"lo"` |
| `str1[:7]` | سلسلة جزئية من البداية للفهرس 7 | `"hello"[:4]` → `"hell"` |
| `str1[3:7]` | سلسلة جزئية من الفهرس 3 للفهرس 7 | `"hello"[1:4]` → `"ell"` |

**ملاحظة:** التابع `str()` يحول أي متحول إلى سلسلة حرفية:
```python
m = 3.1416
mString = str(m)  # "3.1416"
```

### **1.4 أمثلة تعليمية مفصلة**

#### **المثال 1: المقارنة والمعالجة الأساسية**
```python
# إنشاء سلاسل للعمل عليها
s1 = "abcd"; s2 = "ABCD"
s3 = r"Anaconda Platform, provides custom PYTHON development, on-site Training and Consulting;"

# المقارنة الحساسة للحالة
result = (s1 == s2)  # False
print("s1: %s == s2: %s, result: %s" % (s1, s2, result))

result = s1 > s2     # True
print("s1: %s > s2: %s, result: %s" % (s1, s2, result))

result = s1 < s2     # False  
print("s1: %s < s2: %s, result: %s" % (s1, s2, result))

# المقارنة غير الحساسة للحالة
result = (s1.lower() == s2.lower())  # True
print("compare insensitive")
print("s1: %s == s2: %s, result: %s" % (s1, s2, result))

# الدمج والنسخ
s4 = s1 + s2  # "abcdABCD"
print("s6 concatenated from s1 + s2: ", s4)

s5 = s4
print("s5 = s4: ", s5)

# طريقتان للمقارنة
print("\nDoes s5 == s4?: ", s5 == s4)  # True (مقارنة القيم)
print("Does s5 is s4?: ", s5 is s4)    # True (مقارنة العناوين)

# الطول والفهرس
print("\nString s5 is %d characters long." % len(s5))  # 8
print("The 5th character is %s" % s5[4])              # 'A'

# اختبار النهاية
print("s3:%s\nEnds with Training?: %d" % (s3, s3.endswith("Training")))    # 0 (False)
print("Ends with Consulting?: %d" % s3.endswith("Consulting"))              # 1 (True)

# البحث والإدراج
print("\nThe first occurrence of Training in s3 is %s" % s3.index("Training"))  # 63
i = s3.index("Training")
s6 = s3[:i] + "excellent " + s3[i:]
print("s6: %s" % s6)
```

#### **المثال 2: تقسيم النص إلى كلمات باستخدام rindex()**
```python
s1 = "One Two Three Four"

# الحصول على الفهرس الأخير للفراغ
ix = s1.rindex(" ")

# الحصول على الكلمة الأخيرة
s2 = s1[ix + 1:]  # "Four"

# ضبط s1 للجزء من البداية حتى الفهرس ix
s1 = s1[0:ix]     # "One Two Three"

# إيجاد الفراغ الأخير في s1 (بعد "two")
ix = s1.rindex(" ")

# ضبط s3 للجزء من الفهرس ix+1
s3 = s1[ix + 1:]  # "Three"

# إعادة ضبط s1 للجزء من البداية حتى الفهرس ix
s1 = s1[0:ix]     # "One Two"

# إعادة ضبط ix للفراغ بين "one" و "two"
ix = s1.rindex(" ")

# ضبط s4 للجزء من الفهرس ix+1
s4 = s1[ix + 1:]  # "Two"

# إعادة ضبط s1 للجزء المتبقي
s1 = s1[0:ix]     # "One"
s5 = s1

print("s2: {0}\ns3: {1}".format(s2, s3))  # s2: Four, s3: Three
print("s4: {0}\ns5: {1}".format(s4, s5))  # s4: Two, s5: One
print("s1: {0}".format(s1))               # s1: One
```

#### **المثال 3: معالجة بيانات الطلاب من الإدخال**
```python
# مثال بيانات الإدخال: 20510 85 60 80 Sami
id = -1
while id != 0:    # إنهاء الإدخال بالقيمة 0
    print("Data: id, eng, math, prog, name:")
    line = input()
    items = line.split()      # تقسيم السطر إلى قائمة
    id = int(items[0])
    
    if id == 0:
        break
    else:
        eng = float(items[1])
        math = float(items[2])
        prog = float(items[3])
        name = items[4]
        print("{0} {1} Max= {2}".format(id, name, max(eng, math, prog)))
```

**نتيجة التنفيذ:**
```
Data: id, eng, math, prog, name:
20510 80 60 85 Sami
20510 Sami Max= 85.0
Data: id, eng, math, prog, name:
20511 70 23 34 Rama
20511 Rama Max= 70
Data: id, eng, math, prog, name:
0
```

### **1.5 ضرب السلاسل الحرفية (خاصية بايثون)**
```python
print("input k in [2..10] = ")
k = input()  # لنفرض المستخدم أدخل "5"

# جدول الضرب باستخدام ضرب السلاسل
i = 1
while i <= 10:
    print(k, "x", i, "=", k * i)
    i += 1
```

**نتيجة التنفيذ:**
```
5 x 1 = 5
5 x 2 = 55
5 x 3 = 555
5 x 4 = 5555
5 x 5 = 55555
5 x 6 = 555555
5 x 7 = 5555555
5 x 8 = 55555555
5 x 9 = 555555555
5 x 10 = 5555555555
```

### **1.6 تمارين على السلاسل الحرفية**

#### **التمرين 1: تحويل الحرف الأول من كل كلمة لحرف كبير**
```python
# المطلوب: كتابة برنامج يأخذ سلسلة نصية ويحول الحرف الأول من كل كلمة إلى حرف كبير
# الدخل: "I go to school every day"
# الخرج: "I Go To School Every Day"
```

#### **التمرين 2: البحث عن كلمة وعد مرات ظهورها**
```python
S = "what is world wide web : www"
sf = "w"
print("String: " + S)
print("Looking for: " + sf)
print()

ib = iw = nw = 0
while ib < len(S):
    try:
        iw = S.index(sf, ib)
        print("{0} found at index {1}".format(sf, iw))
        nw += 1
        ib = iw + 1
    except ValueError:
        break

print("word ({0}) found {1} times".format(sf, nw))
```

#### **التمرين 3: عرض الترميز Unicode للأحرف العربية**
```python
s = ""
for i in range(1569, 1590):
    s += str(i) + " = " + chr(i) + "\n"
s = "Arabic Unicode\n" + s
print(s)
```

**نتيجة التنفيذ:**
```
Arabic Unicode
1569 = ء
1570 = آ
1571 = أ
1572 = ؤ
1573 = إ
1574 = ئ
1575 = ا
1576 = ب
1577 = ة
1578 = ت
1579 = ث
1580 = ج
1581 = ح
1582 = خ
1583 = د
1584 = ذ
1585 = ر
1586 = ز
1587 = س
1588 = ش
1589 = ص
```

---

## **2. الملفات النصية التسلسلية**

### **2.1 الكتابة في ملف نصي تسلسلي**
```python
fw = open("filePath.txt", "w")  # فتح الملف للكتابة

# الكتابة باستخدام writelines (قائمة سلاسل)
fw.writelines(["First line\n", "Second line\n", "Third line\n"])

# الكتابة باستخدام write (سلسلة مفردة)
fw.write("Hello World of text files!\n")
fw.write("This is a Python example of Text files\n")
fw.write("\n")

fw.close()  # إغلاق الملف
```

**ملاحظات مهمة:**
- `open(path, 'w')` يربط متحول الملف بالملف الحقيقي على القرص
- إذا كان الملف موجوداً سيتم حذف محتواه
- إذا لم يكن الملف موجوداً سيتم إنشاؤه جديداً

### **2.2 الإضافة إلى ملف نصي (Append)**
```python
# فتح الملف للإضافة - لا يحذف المحتوى الحالي
fa = open("filePath.txt", "a")  # 'a' للإضافة

# إضافة محتوى جديد نهاية الملف
fa.write("This line is appended\n")
fa.write("Another appended line\n")

fa.close()  # إغلاق الملف
```

**ملاحظات مهمة عن وضع الإضافة:**
- `open(path, 'a')` يفتح الملف للإضافة
- إذا كان الملف موجوداً، تتم الكتابة في نهايته دون حذف المحتوى الحالي
- إذا لم يكن الملف موجوداً، يتم إنشاؤه جديداً
- المؤشر يبدأ من نهاية الملف دائماً

### **2.3 القراءة من ملف نصي تسلسلي**
```python
fr = open(r"d:\filePath.txt", "r")  # فتح الملف للقراءة

# قراءة سطر بسطر
result = fr.readline()
print("1: ", result)

i = 1
while result != "":
    i += 1
    result = fr.readline()
    print(str(i) + ": ", result)

fr.close()
```

**نتيجة التنفيذ:**
```
1: First line
2: Second line
3: Third line
4: Hello World of text files!
5: This is a Python example of Text files
6: 
7: 
```

### **2.4 طرق متعددة لقراءة الملفات**

#### **الطريقة 1: قراءة كامل الملف دفعة واحدة**
```python
fr = open(r"d:\filePath.txt", "r")
result = fr.read()      # قراءة كامل المحتوى إلى سلسلة واحدة
print(result)
fr.close()
```

#### **الطريقة 2: قراءة كقائمة سلاسل**
```python
fr = open(r"d:\filePath.txt", "r")
result = fr.readlines()  # قراءة كقائمة، كل سطر عنصر في القائمة
for i in range(len(result)):
    print("%d: %s" % (i, result[i]))
fr.close()
```

#### **الطريقة 3: التكرار المباشر على الملف (خاصية بايثون)**
```python
fr = open(r"d:\filePath.txt", "r")
i = 0
for line in fr:         # التكرار مباشرة على أسطر الملف
    i += 1
    print("%d: %s" % (i, line))
fr.close()
```

---

## **3. إدارة السياق باستخدام with (Context Management)**

### **3.1 مقدمة عن with**
كلمة `with` في بايثون تُستخدم لإدارة السياق (Context Management) وتوفر طريقة آمنة للتعامل مع الموارد مثل الملفات، الشبكات، وقواعد البيانات.

### **3.2 لماذا نستخدم with؟**

#### **الطريقة التقليدية (بدون with)**
```python
file = open("example.txt", "w")
try:
    file.write("Hello World")
finally:
    file.close()  # يجب إغلاق الملف يدوياً
```

#### **الطريقة الحديثة (باستخدام with)**
```python
with open("example.txt", "w") as file:
    file.write("Hello World")
# الملف يُغلق تلقائياً هنا
```

### **3.3 فوائد استخدام with**
1. **الإغلاق التلقائي**: لا حاجة لاستدعاء `close()` يدوياً
2. **معالجة الأخطاء**: يدير الاستثناءات تلقائياً
3. **كود أنظف**: يقلل من عدد أسطر الكود
4. **أكثر أماناً**: يضمن إغلاق الملف حتى في حالة حدوث أخطاء

### **3.4 أمثلة عملية مع with**

#### **الكتابة باستخدام with**
```python
with open("data.txt", "w") as file:
    file.write("Line 1\n")
    file.write("Line 2\n")
    file.write("Line 3\n")
# الملف يُغلق تلقائياً
```

#### **الإضافة باستخدام with**
```python
with open("data.txt", "a") as file:
    file.write("Appended line 1\n")
    file.write("Appended line 2\n")
# الملف يُغلق تلقائياً
```

#### **القراءة باستخدام with**
```python
with open("data.txt", "r") as file:
    content = file.read()
    print("File content:")
    print(content)
# الملف يُغلق تلقائياً
```

### **3.5 مثال شامل: معالجة بيانات الطلاب باستخدام with**
```python
# كتابة بيانات الطلاب
students = [
    "20510 85 60 80 Sami",
    "20420 60 80 90 Rana", 
    "20600 95 70 50 Jamil"
]

with open("students.txt", "w") as file:
    for student in students:
        file.write(student + "\n")

# قراءة ومعالجة البيانات
with open("students.txt", "r") as file:
    print("Student Data:")
    for line in file:
        items = line.strip().split()
        if len(items) >= 5:
            id = items[0]
            eng = float(items[1])
            math = float(items[2])
            prog = float(items[3])
            name = items[4]
            max_grade = max(eng, math, prog)
            print(f"{id} {name}: Highest grade = {max_grade}")
```

## **3. الكلمات المحجوزة في بايثون**

### **3.1 الكلمات المحجوزة الكاملة (33 كلمة)**
```
False      await      else       import     pass
None       break      except     in         raise
True       class      finally    is         return
and        continue   for        lambda     try
as         def        from       nonlocal   while
assert     del        global     not        with
async      elif       if         or         yield
```

### **3.2 الكلمات المستخدمة في هذا الفصل**

| الكلمة | الوصف | الفصل |
|--------|-------|-------|
| `in` | للتحقق من وجود عنصر في مجموعة | Ch01 |
| `is` | لمقارنة العناوين والهوية | Ch02 |
| `for` | لإنشاء حلقة for | Ch01 |
| `while` | لإنشاء حلقة while | Ch01 |
| `if`, `elif`, `else` | للتعليمات الشرطية | Ch01 |
| `with` | لإدارة السياق والموارد | Ch03 |

---

## **4. التوابع المسبقة التعريف**

### **4.1 التوابع المستخدمة في الفصل**

| التابع | الوصف | الفصل |
|--------|-------|-------|
| `chr()` | إرجاع محرف من كود Unicode | Ch01, Ch03 |
| `float()` | تحويل إلى عدد عشري | Ch01, Ch03 |
| `int()` | تحويل إلى عدد صحيح | Ch01, Ch03 |
| `len()` | إرجاع طول الكائن | Ch01, Ch02, Ch03 |
| `max()` | إرجاع أكبر عنصر | Ch01, Ch02, Ch03 |
| `open()` | فتح ملف وإرجاع كائن ملف | Ch03 |
| `ord()` | تحويل محرف إلى كود Unicode | Ch01, Ch03 |
| `str()` | إرجاع كائن سلسلة | Ch01, Ch03 |
| `input()` | السماح بإدخال المستخدم | Ch01 |
| `print()` | الطباعة لجهاز الإخراج | Ch01 |
| `range()` | إرجاع سلسلة أرقام | Ch01 |

### **4.2 أمثلة استخدام**
```python
# تحويل بين المحارف والأرقام
print(chr(65))      # 'A'
print(ord('A'))     # 65

# التعامل مع السلاسل
s = "Hello World"
print(len(s))       # 11
print(max(s))       # 'r'

# التعامل مع الملفات
with open("file.txt", "r") as f:
    content = f.read()

# الإدخال والإخراج
name = input("Enter your name: ")
print("Hello", name)
```

---

## **🎯 النقاط الهامة في الفصل الثالث**

- ✅ **السلاسل الحرفية** - تخزن النصوص وتدعم عمليات متنوعة
- ✅ **عدم القابلية للتعديل** - السلاسل في بايثون غير قابلة للتغيير مباشرة
- ✅ **طرائق متنوعة للصياغة** - `+`, `format()`, `%` لكل منها استخداماته
- ✅ **البحث والتقطيع** - `index()`, `split()`, الشرائح `[start:end]`
- ✅ **المقارنة** - حساسة وغير حساسة للحالة
- ✅ **الملفات النصية** - وسيلة تخزين دائمة للمعلومات
- ✅ **أنماط الفتح** - `'r'` للقراءة، `'w'` للكتابة، `'a'` للإضافة
- ✅ **طرائق القراءة** - `read()`, `readline()`, `readlines()`
- ✅ **التكرار المباشر** - يمكن التكرار على أسطر الملف مباشرة
- ✅ **إدارة السياق** - استخدام `with` للتعامل الآمن مع الملفات
- ✅ **خاصية الضرب** - ضرب سلسلة بعدد صحيح يؤدي لتكرارها
- ✅ **التحويل بين الأنماط** - `str()`, `int()`, `float()` للتحويل بين السلاسل والأرقام
---